using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cube : MonoBehaviour
{
    // Start is called before the first frame update
    //void //Start()
    //{
    //    if (Physics.Raycast(transform.position, transform.up))
    //    {
    //        GetComponent<Collider>().enabled = false;
    //        GetComponent<MeshRenderer>().enabled = false; //hace un raycast para arriba y si hay un cubo arriba te desabilita el collider, si hace el raycast y no hay nada es decir los superiores que se ven llevarian collider
    //    }
    //}//Start

}//cube
